# requirements-mlx.txt — pinned deps for the LOCAL MLX QLoRA fine-tune path.
#
# This is a SEPARATE, dedicated environment from the repo's main
# requirements.txt (pandas/pyarrow/numpy/pyyaml, no ML deps) and from
# requirements-finetune.txt (the superseded CUDA/bitsandbytes/HF-Trainer
# stack for the not-pre-approved GPU-rental fallback -- see
# finetune/MLX_FEASIBILITY.md and finetune/README.md's banner).
#
# Install into a dedicated venv, e.g.:
#   python3.11 -m venv finetune/.mlx_venv
#   source finetune/.mlx_venv/bin/activate
#   pip install -r finetune/requirements-mlx.txt
#
# Requires Python >=3.10 (mlx 0.32.1 and transformers 5.15.x both declare
# requires_python >=3.10; the system Python on the owner's Mac was 3.9.6 at
# the time MLX_FEASIBILITY.md was written -- see that doc §1). Installed
# here against Homebrew python3.11.10.
#
# Top-level pins match MLX_FEASIBILITY.md §1's "checked 2026-08-18" versions
# exactly (mlx 0.32.1, mlx-lm 0.31.3). Full resolved environment recorded
# below (`pip freeze` output, installed 2026-08-20) for exact reproducibility.

mlx==0.32.1
mlx-lm==0.31.3

# --- full resolved environment (transitive deps), pip freeze, 2026-08-20 ---
# annotated-doc==0.0.5
# anyio==4.14.2
# certifi==2026.7.22
# click==8.4.2
# filelock==3.32.3
# fsspec==2026.7.0
# h11==0.16.0
# hf-xet==1.6.0
# httpcore==1.0.9
# httpx==0.28.1
# huggingface_hub==1.28.0
# idna==3.19
# Jinja2==3.1.6
# markdown-it-py==4.2.0
# MarkupSafe==3.0.3
# mdurl==0.1.2
# mlx==0.32.1
# mlx-lm==0.31.3
# mlx-metal==0.32.1
# numpy==2.4.6
# packaging==26.3
# protobuf==7.36.0
# Pygments==2.21.0
# PyYAML==6.0.3
# regex==2026.7.19
# rich==15.0.0
# safetensors==0.8.0
# sentencepiece==0.2.2
# shellingham==1.5.4
# tokenizers==0.22.2
# tqdm==4.70.0
# transformers==5.15.1
# typer==0.27.1
# typing_extensions==4.16.0
